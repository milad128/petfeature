# Test / development dependencies — NOT installed in the production image.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
#
# httpx is already pinned in requirements.txt and is reused as the test client.

pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
