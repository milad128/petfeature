[tool.pytest.ini_options]
# Async tests run without an explicit @pytest.mark.asyncio decorator.
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
# Make `import app...` resolve without installing the package.
pythonpath = ["."]
addopts = "-ra"
